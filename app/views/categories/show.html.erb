<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<p>
  <strong>Icon:</strong>
  <%= @category.icon %>
</p>

  <ul>
    <% @category.items.each do |item| %>
    <li><%= item.title %> – <%= item.icon %> – <% if item.stock? %><%= item.stock %><% else %>0<% end %> <%= link_to 'update', change_stock_items_path(:id => item), remote: true %></li>
    <% end %>
  </ul>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
